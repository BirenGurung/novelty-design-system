# Badge

Use for status or count (e.g. notifications). Variants: default, success, error, info, warning, outline. Size: small, default.

## Props

- `variant`, `size`, `onDismiss`, children
- Standard span attributes

## Example

```tsx
<Badge variant="error" size="small">3</Badge>
<Badge variant="outline">New</Badge>
```
